// object type

var employee:{name:string;                          //object
    age:number;
  }=
  {
      name:"ambika",
      age:34
  }

  console.log(`(object) employee name: ${employee.name}`)
  console.log(`(object) employee age: ${employee.age}`)

// empty object
var users:{}={}
// users.name;     //error throws because it is an empty object

